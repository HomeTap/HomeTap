extends layout

block content
  header
    h1 Home Tap

  
  div
    .left
      p User Queues
      p Settings
  
  div
    .right
      .button
        a(href='/admin/beers')
          button(type='button') Manage Beer Listings
      div.form
        form(method='GET', action='/user')    
        p User Name / Next Beer Name / button(type='button') Send Beer
        p User Name / Next Beer Name / button(type='button') Send Beer
        p User Name / Next Beer Name / button(type='button') Send Beer

 
