extends layout

block content
  header
    h1 Home Tap
  if (!user)
    p
      a(href='/login') Login
    p
      a(href='/register') Register
  else
    h1 Welcome to HomeTap
    p Current user: #{user.username}
    a(href='/logout') Logout
    